# Deliberately privileged runtime profile for owner-operated XiaoBa hosts.
#
# Keep application paths, EnvironmentFile, ExecStart, and resource limits in
# the base xiaoba-dashboard.service unit. This drop-in changes only the
# execution identity and privilege-escalation boundary.
[Service]
User=root
Group=root
NoNewPrivileges=false
