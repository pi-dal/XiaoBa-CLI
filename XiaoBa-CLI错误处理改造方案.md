# XiaoBa-CLI 错误处理改造方案

- 日期：2026-08-05
- 当前状态：本地实现与针对性验证已完成，尚未部署
- 本阶段范围：只处理模型调用失败后的错误读取、分类、重试衔接和用户文案

## 1. 当前问题

虚拟员工调用模型失败后，XiaoBa 本应读取并记录真实错误，例如超时、限流或上游服务异常。

当前 Responses API 使用流式响应。收到非 2xx 响应时，Axios 的 `error.response.data` 可能是网络流对象。现有代码在判断是否需要关闭 Prompt Cache 并重试时，直接对该对象执行 `JSON.stringify`，导致循环引用异常：

```text
Converting circular structure to JSON
```

这个二次异常覆盖了最初的模型或中转 API 错误。系统因此无法获得真实状态码和错误正文，只能把它分类为未知错误，并向用户返回统一兜底文案：

```text
不好意思，刚才处理出了点问题，你再试一次？
```

## 2. 当前链路

```text
用户发送消息
  → CatsCo 将消息交给 XiaoBa
  → XiaoBa 恢复会话并构造模型请求
  → OpenAIProvider 通过中转 API 调用模型
  → 中转 API 或模型返回错误
  → Axios 提供状态码和流式错误响应
  → XiaoBa 判断是否关闭 Prompt Cache 后重试
  → 对错误流执行 JSON.stringify
  → 发生循环引用异常
  → 原始错误被覆盖
  → 无法正确分类和重试
  → 返回统一兜底文案
```

确定的代码问题位于 `src/providers/openai-provider.ts` 中的 `shouldRetryWithoutExplicitCaching` 错误分析过程。

## 3. 当前判断

已经确认：

- XiaoBa 的错误处理代码存在确定性缺陷。
- 错误处理过程可能覆盖原始 HTTP 状态码和错误正文。
- 现有测试没有覆盖“非 2xx 流式错误响应”的情况。
- 用户目前看到的文案不是某个具体错误对应的文案，而是无法分类时的最终兜底。

尚未确认：

- 最初的模型请求为什么失败。
- 故障来自中转 API、实际模型供应商，还是请求本身。
- `grp_891` 的大上下文是否是最初失败的直接原因。

这些未知项不阻塞本阶段开发。本阶段先让 XiaoBa 能够正确获取和处理真实错误。

## 4. 本阶段改动目标

### 4.1 安全读取错误响应

错误处理需要识别以下数据类型：

- 字符串：直接读取；
- 普通 JSON：安全序列化；
- Buffer：限制长度后转换；
- Stream：有界读取错误正文；
- 循环对象或异常对象：安全降级，不能再次抛出异常。

错误流需要设置最大读取长度，避免错误响应本身过大。初步建议最多读取 64 KB，并在超出时截断。

### 4.2 保留内部错误信息

内部日志和错误对象需要尽量保留：

- HTTP 状态码；
- 错误正文摘要；
- request-id；
- Axios 或网络错误码；
- 原始错误对象或 `cause`；
- 实际重试次数。

这些信息只用于内部分类、重试和日志，不直接展示给用户。

### 4.3 保留 Prompt Cache 兼容逻辑

当错误正文明确表示不支持 `prompt_cache_options` 或 `prompt_cache_breakpoint` 时：

1. 删除显式缓存参数；
2. 自动重试一次；
3. 如果重试成功，用户不看到错误；
4. 如果仍然失败，再按照最终错误类型返回文案。

不能简单把错误流替换为 `[stream]`，否则可能看不到缓存参数不兼容信息，破坏现有自动降级逻辑。

### 4.4 衔接现有重试策略

修复后，真实状态码需要继续交给现有重试策略处理：

- 临时网络或服务错误：按现有规则自动重试；
- Prompt Cache 不兼容：关闭缓存参数后重试；
- 明确不可重试的错误：停止重试并返回对应文案；
- 未知错误：安全结束，不得产生二次异常。

只有实际发生过自动重试时，用户文案才提示“系统已自动重试”。

## 5. 用户文案规则

不向用户展示 HTTP 状态码、内部错误码或错误编号。用户只看到通俗、可行动的提示。

| 错误类型 | 用户文案 |
|---|---|
| 请求超时 | 模型响应超时，本轮上下文已保留，请稍后继续。 |
| 请求超时且已重试 | 模型响应超时，系统已自动重试，但仍未完成。本轮上下文已保留，请稍后继续。 |
| 服务临时异常 | 模型服务暂时不可用，请稍后再试。 |
| 服务临时异常且已重试 | 模型服务暂时不可用，系统已自动重试，但仍未恢复，请稍后再试。 |
| 请求受限 | 当前请求较多，请稍等片刻再试。 |
| 额度不足 | 当前模型暂时无法继续调用，请切换模型或联系管理员。 |
| 上下文过大 | 当前对话内容较多，需要整理上下文后继续。 |
| 配置或权限异常 | 模型服务配置异常，请联系管理员处理。 |
| 未知错误 | 本次处理未能完成，请稍后再试。 |

文案分类依据保留在内部，用户文案不直接绑定或展示 429、502、504 等技术状态码。

## 6. 本阶段边界

本阶段会处理：

- 流式错误响应的安全读取；
- 原始错误信息保留；
- 错误分类；
- 与现有自动重试逻辑衔接；
- 分类后的用户文案；
- 相关回归测试。

本阶段不会处理：

- 修改中转 API 或模型供应商；
- 调整 Bifrost 的超时、额度或请求限制；
- 清理 `grp_891` 会话；
- 修改上下文压缩策略；
- 调整模型窗口或 Prompt Budget；
- 全量部署所有 worker。

## 7. 实施步骤

1. 增加测试，稳定复现非 2xx 流式错误响应导致的循环引用异常。
2. 实现统一、安全的错误响应读取和规范化。
3. 修改 Prompt Cache 兼容判断，使其使用规范化后的错误信息。
4. 确认真实状态码能够进入现有错误分类和重试逻辑。
5. 实现分类用户文案，并根据实际重试次数动态选择文案。
6. 补齐超时、临时异常、请求受限、缓存不兼容、循环对象和未知错误测试。
7. 完成构建和测试后，只在一台 worker 上验证。
8. 验证能够记录真实错误后，再决定是否处理上下文或中转 API 问题。

## 8. 验收标准

- 非 2xx 流式响应不会再触发循环引用异常。
- 错误处理过程在任何输入下都不会覆盖原始错误。
- 内部能够获得并记录真实状态码和有限长度的错误摘要。
- Prompt Cache 不兼容时仍能自动关闭缓存参数并重试。
- 临时错误能够按照现有策略重试。
- 用户只看到对应类型的通俗文案，不看到技术状态码或错误编号。
- 只有实际重试过的请求才提示“系统已自动重试”。
- 所有新增回归测试和项目构建通过。

## 9. 完成本阶段后的预期结果

本阶段不保证上游模型请求一定成功，但能够保证：

```text
上游请求失败
  → XiaoBa 正确读取真实错误
  → 按错误类型决定是否重试
  → 内部日志保留定位信息
  → 用户收到对应场景的通俗提示
```

完成后，再根据暴露出的真实错误决定是否需要处理大上下文、会话压缩、中转 API 或模型服务问题。

## 10. 本地实现结果

已完成：

- Responses API 的错误流会在进入缓存兼容判断前进行有界读取；
- 错误正文最多读取 64 KB，超出后截断；
- JSON 错误正文会恢复为结构化对象，纯文本错误会保留为消息；
- 取消请求不会等待错误流读取；
- 循环对象不会再导致错误处理过程二次崩溃；
- 原始状态码、响应头和 request-id 仍保留在错误对象中；
- Prompt Cache 不兼容时仍会关闭显式缓存并自动重试；
- AgentSession 已使用错误分类器返回对应的通俗文案；
- 只有内部重试记录确认发生过重试时，文案才提示“系统已自动重试”；
- 飞书和微信渠道改为根据 `taskOutcome=failed` 投递失败文案，不再依赖旧兜底文案的精确字符串。

本地验证：

- TypeScript 构建通过；
- 错误处理、AIService 重试、AgentSession 文案、飞书和微信投递相关的 80 个测试全部通过；
- 完整 runtime 套件中，本次改动相关测试全部通过；仍存在 Windows 环境下的既有测试问题，包括临时目录清理、路径断言和 GrepTool 取消信号用例，这些文件未被本次改动修改。

下一步建议先在单台 worker 上部署验证，确认真实错误状态和文案符合预期后，再考虑同步其他 worker。
